<h1>タスク 編集</h1>
<%= form_with model: @task, url: "/tasks/#{@task.id}", method: :patch, local: true do |f| %>
  <p><%= f.text_field :title %></p>
  <p><%= f.text_field :content %></p>
  <%= f.submit "送信" %>
  <p><%= link_to "一覧画面へ", tasks_path %></p>
<% end %>
